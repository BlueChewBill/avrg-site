# AVRG site — working notes for Claude

> **This repo is PUBLIC.** Everything committed here — including this file — is readable by anyone. Keep business rationale, personal context, and anything sensitive out; that belongs in Claude's local memory, which points here for project mechanics.

Static site for AVRG handmade fingerboards. No framework, no npm — plain HTML/CSS/JS plus two Python scripts. Media is committed directly (no LFS).

## Commands

- `python3 build_site.py` — rebuild `site/img/` + `site/data.js` from photos. Re-run any time photos in `boards/` or `completes/` are added/removed/moved.
- `python3 seed_boards.py` — one-time seed of `boards/` from the raw photo folders; safe to re-run (skips populated collections).
- Preview servers (`.claude/launch.json`): `avrg-site` serves `site/` on :8765; `avrg-root` serves the repo root on :8123.

## Layout

- `site/` — current live site source. `site/data.js` is **generated** by build_site.py; don't hand-edit it.
- `redesign/` — active explorations only. **`h-blackout.html` is the active direction.** Checkpoints are saved as `h-blackout.<date>-checkpoint.html`. Superseded drafts (`a-`…`g-`, the `i-` pasteups, `header-playground.html`, old-site backup) were moved to `retired/` at repo root — note their relative `../site/` asset paths assume they'd be served from one level down, so retired pages may render without data/images; that's fine, they're archives. `redesign/card-lab.html` is the product-card treatment explorer (A–B photo controls vs C–F background-removed cutouts; sample cutouts in `redesign/card-lab/`, made with the `cutout.swift` + `trim.swift` scripts there — `swift cutout.swift in.jpg out.png` then `swift trim.swift out.png`; macOS Vision, no deps). **`redesign/card-lab/cuts/` holds the full batch — one cutout per board (39/39), cut from `site/img/full/`; re-run the swift pair for new boards.** `redesign/j-cards-black.html` / `j-cards-white.html` render the card-lab finalists in the real site frame (topbar + collection sections, live data.js) on the blackout ground and the inverted white ground; bottom-right switcher flips treatments, selection lives in the URL hash (`#s-spread` etc). Their dims/status/sold info rows are placeholder demo data. The white page's ⸺·⸺ frame card is the lead candidate and has three interaction variants — `#s-frame` (chip boxes wipe in from the frame edge, slide back out), `#s-framev` (exit collapses onto the chip's centerline), `#s-framev3` (exits recede into the frame rails + whole card at half speed) — sharing one hover system: corner chips flush to the inner frame, ref decodes to AVRG, name decodes to dims (only boards in the `PH_DIMS` map), status is a dot whose word decodes in. Gotcha baked into v2/v3: enter and exit animate on different axes, so BOTH are keyframes and the exit is armed by a JS-applied `.left` class (a transition can't pick up from an animation-held value, and an unarmed exit animation would fire on page load).
- `redesign/k-home-dual.html` — the h-blackout home page (same engine, conveyor shelves and all) with two live switches bottom-right: black/white **ground** (a working prototype of a site light/dark mode) and a **card** selector (Photo = h baseline, Frame v3, Paper, Zoned — the j-cards finalists riding the real conveyor + collection grids, with cutouts, demo statuses, and the frame card's full decode choreography). Both persist in localStorage (`avrg-k-ground`, `avrg-k-card`); 2×4 combinations. Dark is the h-blackout CSS verbatim; light is a `body.light` override block using the j-cards-white idiom (#fff ground, #000 max-contrast, #444 soft, #767676 hint). Treatments keep their own ground on both page grounds by design (the collision is the point); the only adaptations are fluid sizing (width 100%, square stage, so one card fits belt slots and grids), a taller belt (`body:not([data-card="photo"])`), an ink border on zoned in light mode, and a belt furniture pass (`.card .wchip/.st/…`) for the d5/paper cards — the d7f's hand-scaled belt pass retired onto the cqw spec (see ONE CARD SPEC below). While frame v3 is selected, the lightbox becomes the frame v3 card blown up (`#lb.fv3` CSS + `.d7f` class on `#lb-card`, so the delegated hover choreography — rails, chip wipes, ref/name/status decodes — runs on the big card): cutout hero on open, thumbs/arrows step to real photos, product box slims to the DM cta. Nav arrows live ON the card at the mid-rail dots (sprint item 1, done): seat = flush square chip tucked to the rail (DECIDED — empty circles clashed with the card's angular language; the fill-disc variant was built and rejected). One gesture on card hover (~.94s, board+shadow growth spans it): rails close onto the dots → line grows from each dot (SVG dash) → seat draws as two half-rects meeting at the far side → seat fills like a chip (chipwipe, horizontal from the rail toward card middle) and the arrow flips white. Exit drains vertically into the frame (chipdrop via the shared `.left` class; prev up, next down, keeping the card's diagonal). Arrow's own hover inverts (white fill, inset ink ring, black glyph). Stock pucks hide in fv3. Other card styles keep the stock lightbox on purpose. Light-mode logo handling: the bar swaps to `logo-circle.png`, the masthead keeps `logo-line-white.png` under `filter: invert(1)`. **Card images + refs (2026-07-21): the `CANVA` map near the top of the JS points each site board at its Canva cutout (`card-lab/canva/cl<N>/hs<N>-top|bottom.png`), and card refs are derived from that map (`INVREF`) — the Canva sheet numbering is the canonical inventory ID, so refs are non-contiguous on purpose (classic-01 = CL 3). data.js's site-ordinal refs are only the fallback (originals — no canva batch, they keep Vision cuts). Mapping receipts: `card-lab/canva-review.html`. Demo-state maps (`JSPARK`/`JGONE`/…) are keyed by the NEW refs.** Gotchas: the lightbox frame is `object-fit: contain`, so its fill color shows as letterbox bands — near-black on dark, white on light (anything else reads as a stripe); the lightbox pull-forward animation looks for `.frame, .panel` since j treatments have no `.frame`. **ONE CARD SPEC (2026-07-22): `.d7f` is a CSS container (`container-type: inline-size`) and every internal dimension is cqw** — a px line at the 232 reference precedes each cqw line as a pre-2022-Safari fallback (old browsers get the previous approximate look). Grid slots still render via `lockCardScale` (232 then transform-down — true reductions that duck minimum-font clamps); belt slots and the fv3 lightbox card are exact reductions/enlargements by construction, so the hand-scaled `.card .d7f` belt pass and the `#lb.fv3` geometry overrides are DELETED — `#lb.fv3` keeps only the ambient shadow, frame-chrome kill, nav, and product slimming. The lb `.frame` rides the same square spec as `.stage` (shared `.d7f .stage, .d7f .frame` selectors; img abs-centered at max 79%/78% — the iOS-safe pattern); the nav seat/arrows are cqw at a 430 reference (N px at 430 = N × 0.232558cqw) so the seat tracks the rail dots at any card width. Card aspect ≈ 1.51:1 everywhere → the open/close flight anchors whole-panel-to-panel in fv3 (`anchors()` helper; other treatments keep square-to-square vs the stock lb) and lands every edge — this is what fixed the mobile bottom-off landing and the desktop double-frame mid-flight. The lift-out (slot goes empty, card flies back to it) runs on EVERY pointer now, and `#lb` is the mobile scroll-sheet on ALL sizes: below the live topbar, transparent scrim, flex-centered card, plain scroll on overflow, with a desktop height guard (`max(300px, min(430px, (100dvh − tbh − 120px)/1.53))`) that shrinks the whole card via cqw instead of clipping. `#vlb` keeps the stock dark-scrim centered layout on desktop.
- `redesign/dock-add-lab.html` — mobile dock add-animation study tool (decode-slowmo's sibling, not a site page): fake phone, one master p over a Catmull-Rom spline through beat poses. THREE path variants on a switcher (ramp = Dylan's drawn beats: tilted drop → bottom-left ramp → low sweep → top-right crest → feather; toss = pull-back throw over a center apex; swoop = convex climb up-left growing the whole way → concave return), each with its own saved knob preset (`PRESETS`, baked 2026-07-21 — switching paths stashes/loads automatically). Size is a field over the screen (max at center-x, side-shrink knob). Landing = button-press squash about the card's bottom edge + shadow compression + synthesized thunk (Web Audio, sound checkbox). Scrub-p slider freezes any frame. Tune here, then re-bake numbers into k-home-dual's `DOCK_TUNE`.
- `redesign/card-lab/batch-review.html` — contact sheet for unmapped Canva export batches (`canva/batch1-pNN.png`) with pixel-matcher name guesses; rename files to `<site-name>.png` and edge-compare.html picks them up.
- `redesign/decode-slowmo.html` — decode-effect study tool (huge type, not a site page): sliders for glyph flicker (30–150ms) and swap time (0.25–5s), position-lock mode (per-char cells frozen at final widths), and per-leg direction options: L→R, R→L, In, Out, Collapse (corrupt ends→center then resolve center→out), Sweep (2-glyph head consumes R→L then prints L→R), Sweep 2 (scan full-width → resorb to seed → extend glyphs to full footprint → reveal front eats them), Clps 2 (corrupt in from edges → collapse to center seed → grow to new footprint → resolve center-out). Built because the tagline rotation will get the same treatment — port winners from here.
- `boards/`, `completes/`, `clean shots/` — photography. In a collection folder: a loose image = single-photo board; a subfolder = one board with multiple angles. Originals build from `completes/` and require a description `.txt` per board folder.
- `deploy/` — deployable snapshot (assembled separately; build_site.py does not write here). Actual deployment = drag `site/` to Netlify Drop.
- `videos/web/` — web-ready riding clips (clip-1/2.mp4 + posters); raw `.mov`s are gitignored.

## Gotchas

- Redesign drafts load `../site/data.js` and `../site/img/`, so they only work served from the **repo root** — use the `avrg-root` launch config (:8123), not `avrg-site`.
- Reddit handle and contact email live at the top of `build_site.py` (`REDDIT_HANDLE`, `CONTACT_EMAIL`) and power every DM/contact link.
- The blackout draft's tabs/nav are injected by JS (`#topnav` innerHTML) — tab styling changes are CSS/class work, not markup edits.
- Hover FX conventions in h-blackout: tabs/buttons get the JS decode scramble; DM buttons wear the CSS glitch. The scramble mutates textContent, so it can't be combined with effects that clone or split the label.
- Blackout palette: bg `#000`, bone `#ddd1b6`, ink `#201a15`; per-collection accents orange `#d8511f`, blue `#1f6fb8`, gold `#c98a12`, purple `#6d4fa3`. Fonts: Bricolage Grotesque (display), Space Mono (labels).
- The blackout draft references `../site/` and `../videos/web/` — when porting it to production `site/`, copy those assets in or fix the paths.
- Previewing via `file://` shows stale snapshots in the Claude Code browser pane — always serve over HTTP (`avrg-root`, :8123).
- **iOS Safari ignores `max-height: N%` (with `height:auto`) on in-flow children of `aspect-ratio`-sized boxes** — the image renders intrinsic-size and blows out the card; desktop Chrome resolves it fine, so the browser pane will NOT catch it. Fix: `position:absolute; inset:0; margin:auto` on the child (percentages then resolve against the laid-out box). Definite `height: N%` (like `.frame img`'s 100%) is fine. k-home-dual's stage/tile imgs are fixed; j-cards-black/white still carry the raw pattern — re-check when porting.
- Pushing image batches over HTTPS used to time out ("Recv failure") — repo git config now pins `http.version HTTP/1.1` + 150MB `http.postBuffer`; keep that if the remote ever changes.
- **The card lightbox has no visible scrim on ANY size (universal since 2026-07-22)** — `body.lb-fade` fades `#home/#colpage` to 0 over the page's own ground (topbar stays live above the sheet), so the lifted card's empty slot stays visible during the flight. Born on mobile: every overlay variant banded at the iOS bar (translucent tints read lighter behind the bar chrome; `position:fixed` inside the scrollable lb sheet mispins while scrolling). The `.lb-scrim` element survives only as the transparent tap-to-close target (absolute, full sheet height). The video lightbox (`#vlb`) keeps the dark scrim on desktop.
- Board dimensions are deliberately unlisted for launch — don't add specs unprompted.
- Accent color on the lightbox selected states (`.lb-list-btn.on`, thumb highlight border) is kept deliberately — ask before removing.
- Reusable snippets/fonts/palettes live outside the repo in `~/Projects/web-assets` (see its README index).

## Open threads

- Card direction: pick the winning frame-card interaction variant (j-cards-white `#s-frame` / `#s-framev` / `#s-framev3`) and the winning decode directions (decode-slowmo.html), then port into the card page for real and design the tagline rotation with the same picks. Real dims data feeds the `PH_DIMS` map as boards get calipered.
- Ground direction: black vs white for the home page (`k-home-dual.html` switcher) — may become an actual light/dark mode toggle on the site rather than a one-or-the-other pick.

- **Next sprint (specced 2026-07-20, not started) — fv3 interaction suite in k-home-dual.** Frame v3 is the card direction regardless of ground. Three pieces, in rough order:
  1. *Lightbox nav arrows onto the card.* Move the ‹ › pucks from outside the card onto it, centered on the two mid-rail dots (`.dgl`/`.dgr`). Choreography: on card hover the rail fill closes onto the dot as now; then the line keeps growing FROM the dot toward the newly placed arrow, and splits to draw a circle around it. Hovering the (clickable) arrow fills the circle dark with the text/color swap — same treatment as the +List chip and ref chip.
  2. *Card bay = cart ("the deck"), right screen edge — BUILT in k-home-dual (2026-07-20), iterating.* Progress-driven drawer, not class states: a rAF driver advances one master p (0..1) and poses every piece per frame (`BAY_TUNE` in the JS = speed/segment/threshold knobs; `.bay` CSS vars = geometry). At rest just the inner frame marks (two rails + mid dot, trimmed 72px top/bottom, page-colored). Proximity (195px zone) is the motor: p always climbs in-zone but rate scales with distance — linear ramp over the first 3/4 of the zone, quadratic steepening near the page edge (slow crawl ~6.5s parked at the bound, ~1.1s at the edge, so approaching IS pulling the drawer). Inner line slides at constant speed, never slows; outer frame accelerates to catch it; horizontal motion is ONE eased distance split panel/drawer arithmetically (the corner is not a motion boundary — this killed a stutter, don't reintroduce segments). Face halves draw continuously and only connect at max visibility; shadow + count chip gate on that landing. Click = speed up + lock; inner arrow = quick shut; no clicks = slow drain. Lightbox open → the "lean": marks grow to full height and pull 20px off the edge (yields to the drawer via max()). +List chips are live: cutout flies onto a shelf (or the resting dot, answered by a dot pulse), and the chip label decodes to a random WANT/LIKE/NEED/MINE on hover.
     SEATING REWORKED (2026-07-21): the +List flight is now a rAF-driven arc (`bayFlight` — position/scale/rotation/shadow all posed from one p; quadratic bezier, shadow narrates altitude) that lands on the real shelf item's MEASURED rect — the item renders first hidden (`.preseat`), the clone becomes it in the same frame, no crossfade seam, and the count chip holds until touchdown (`pendingSeat`). Landing answers: shelf-shadow thunk, rack recoil, count bump. Three seat variants on a temporary SEAT switcher row (`avrg-k-seat`, knobs in `SEAT_TUNE`): **drop** (peak hangs over the slot, 9px fall, whisper of squash), **skid** (shallow arc, 19px overshoot, friction slide home), **seatdraw** (the slot draws its seat line while the card is inbound — nav-arrow language). Awaiting Dylan's pick; closed-drawer adds ride the same arc to the resting dot.
     NEXT for the bay: real drag-and-drop of cards into the drawer (trigger = proximity + drag release); while a board is under inspection the invite stays live over the scrim (deliberate — likely the "add to deck from the lightbox" flow). How the deck behaves on the home screen is still undecided.
  3. *Card announces draggability without a "hold to drag" label.* Stage 1 (hover): board AND its drop shadow grow; card stays static but the board leans subtly toward the cursor — cursor acts as a mini invisible sun with gravity on board + shadow, shadow extending opposite the lean. Add an "aim assist" window that damps actual cursor speed near the card (ramped curve toward/away from board center) so the board feels heavy. Stage 2 (click): the card grows slightly to match the already-zoomed board, the card's shadow grows, and the card shadow starts reacting to cursor position the same way the board shadow did on hover. Everything must read quietly — the bar is "doesn't corrupt the clean cards."

- **Mobile pass 1 — shipped 2026-07-20 in k-home-dual.** Frame v3 is now the page default (localStorage still wins); the ground/card switcher collapses to an `SW` chip under 940px; the mobile home tiles are collection cards in a frame-idiom lite (white card, inner frame, count chip AND accent-dot "N BOARDS" line — both counts kept on purpose); no collection has its own page anymore — `renderColPage` renders the selected collection first then every other section below it, one scroll, on all screen sizes (desktop shares the renderer); on touch the fv3 lightbox corner chip decodes to `n/N` position on open and every swipe (swipe-sesh wayfinding, scoped to the collection chunk; desktop keeps ref→AVRG hover). **NEXT: mobile polish sprint — mobile is what most visitors see.** Open design questions from the pass: name-over-board legibility on busy cutouts (top-bold / bottom-bold are the fallback variants), and the collection-card decode flourish (name → "N BOARDS AVAILABLE") needs a touch trigger decision: scroll-into-view once, idle cycle like the taglines, or on-tap-before-navigate. DONE 2026-07-22: **mobile inspection rework** — tap = the hover: `.tset` on the lb card lands the chip fills/rail close/status decode as the zoom settles (dock gate). The tapped grid card lifts out (`visibility:hidden`, slot stays empty) and the close flight lands back in that slot — since 2026-07-22 anchored whole-panel-to-panel (the ONE CARD SPEC made the aspects equal; the original square-to-square anchor existed only because the two specs' aspects differed, which is also why frame↔panel used to land low), and the lift-out runs on desktop too. Card is the hero: desc/product/count hidden ≤720 (DM path = lisst), `.lb-rest` snaps away in .1s on close so thumbs don't ride the return. Sheet starts below the still-live topbar (`--tbh` measured at boot/resize) and flex-centers the card (`margin:auto` degrades to scroll on overflow). **fv3 card got top room** (22.41% of card width — 52px at the 232 reference) so the board sits optically centered — and since 2026-07-22 the `.corner` chip is a `.panel` child in both the renderer template and the lb markup (the stage-anchored negative-top compensation is retired). DONE 2026-07-22: **touch/pointer behavior forks are capability-keyed, not build-split** — all card `:hover` choreography (chip wipes, rail closes, board grow, panel shadows, wchip/bchip inverts, lb image grow) sits in `@media (hover: hover)` gates, mirroring the JS `canHover` gates. Reason: iOS sticky-hover fired the chip wipes as a truncated prelude on tap before the lightbox opened. Touch gets its own scripts (lb n/N decode, dock flight) instead. Pattern for future mobile-only behavior (scroll shadows etc.): add it behind `(hover: none)` / `dockActive()`, never a separate build. DONE 2026-07-21: **card proportion lock** (`lockCardScale`) — shelf-grid cards render at the 232px desktop reference width and transform-scale down to their slot, so narrow-grid cards are photographic reductions of the desktop card (one spec, no mobile override block; desktop slots are 232px so it's a no-op there). Gotchas: call it only when the grid is visible (hidden grids measure 0 → scale(0); route() re-runs it after unhiding), and the mobile shelfgrid tracks must be `minmax(0,1fr)` or the unscaled 232px card balloons the track's auto min-content and the page scrolls sideways. Follow-up DONE 2026-07-22: the d7f belt pass retired onto the cqw ONE CARD SPEC (the `.d5`/`.paper` belt furniture pass remains hand-tuned).
- **Mobile deck ("the dock") — specced 2026-07-20, not started.** Mobile doesn't get a smaller drawer, it gets a different organ; decisions from the scoping session:
  - *Topbar icon* replaces the lisst text on mobile: receipt glyph is the lead candidate (the count badge does the conventional "cart" work, so the glyph is free to have character). Empty-state escalation: first tap decodes to a red numerical **0**; a second tap while still empty gets an annoyed shake.
  - *Adds happen only from the zoomed card page* on mobile (no grid-level +List chips — thumb mis-tap territory, and tap-card → product view → add is the expected path). Confirmation is the topbar icon reacting: shake + count bump. Site-fade on inspection and the card animations stay; grid is 2 cards wide minimum before zoom.
  - *The dock itself*: right edge (matches desktop, dodges the iOS back-swipe), slides open and takes over the screen — a display case made for the cards. Presentation, not a cart page.
  - *Add animation*: fully automated, no variance, beats overlapped. Choreography explored in `dock-add-lab.html` (see Layout — three path variants with saved presets). **DOCK-LITE SHIPPED in k-home-dual (2026-07-21):** the lab flight runs on the real mobile layout. `#kdock` = right-edge display-case sliver (exists only during the flight for now; full takeover case still to build). Receipt icon + count badge replaces the My Lisst text tab on mobile (`.picon`, links #my-lisst, shake+bump on add — note it sits UNDER the open lightbox scrim, so drawer+thunk are the visible confirmation mid-lightbox). Engine: `dockAdd()` — the lab's spline driver with all three paths, presets baked as `DOCK_TUNE`, coordinates scaled from the lab's 390×720 to the viewport, start pose MEASURED from the real card rect (bayFlight's lesson), landing press/shadow/thunk ported. A temporary **Dock row** in the SW switcher flips ramp/toss/swoop (`avrg-k-dock`) — retire like the Seat row once one wins. All three add entry points route through it when `(hover:none) or ≤940px`: fv3 lightbox chip, plain lightbox button, grid chips; desktop keeps flyToBay. Reduced-motion or mid-flight adds get icon-ping only. Tuning loop: retune in the lab → re-bake `DOCK_TUNE` (site reads baked numbers, no live knobs).
  - *Drag-to-deck (desktop)* is an earned shortcut: unlocked after the first proper add via the button. Snappy, not ceremonial — a drag is the user performing, ceremony would fight their hand. Feedback: drawer pulse + icon shake.
  - *Tone law* (general, decided here): when a ceremony risks getting tiring, never shorten or hide it — escalate the absurdity at the same speed. Reserved variant for add-fatigue: wild spin into place, slam shut, sparkly rainbow trail, over as fast as it happened.
- Header cleanup: replace the static topbar buttons with reveal animations / dropdowns speaking the same visual language as the cards (rail draws, chip wipes, decodes) — not yet designed.
- Home page logo: the large line logo needs motion to match the cards — the gaps in the line art should animate together at the dot (same converge language as the card rails / bay marks), plus some kind of movement. Tone check for all of this: over-the-top is on-brand (AVRG = "average" / "a very real goober" — the joke is a maximal site for a humble name). The site's thesis: the cards and coherent animation are what keep a visitor past 5 seconds; the polish is the setup and "it's just a fingerboard, man" is the punchline — the intended discovery ladder is angry cart → a "MINE!" decode → actually reading the taglines, at which point *you know that he knows*.
- Caliper specs + nicknames for the 19 Classics (descriptions live in build_site.py / per-board `.txt` files).
- Video section for the riding clips in `videos/web/` (transcoded, waiting on a home).
- Button chrome visual direction — the explorer (`retired/header-playground.html`) is retired; interaction layer (decode/glitch) already shipped. Dylan has been collecting button references while browsing — expect a reaction session (show variants, don't ask for a spec).
- Canva cutout pipeline — **CLASSICS DONE (2026-07-21): all 54 classics exported, bg-removed, pre-named as `redesign/card-lab/canva/cl<N>-top.png` / `cl<N>-bottom.png`** (107 files; cl54-bottom missing — that photo was never taken, re-shoot the teal board's underside). The winning flow (repeat per collection): (1) Canva **Sheet** with columns name/size/top/bottom, one board per row, bg-removed images in the cells; (2) name the sheet after the collection (design titles inherit it); (3) sidebar **Bulk create** → data range incl. header row → pick `classics-card-template` (2000×1500, two images, NO text element — a text layer bakes into the export and pollutes the cutouts, learned the hard way) → auto-match → generates one design per row titled `(Bulk N) <sheet-name>`; (4) `search-designs` for those titles (paginate, ~25/call), `export-design` each `{png, transparent_background, lossless}`, curl down; (5) local script slices each card L/R and keeps the largest connected alpha component per half (scipy `ndimage.label` — this also strips any stray marks), crops to bbox, writes `cl<N>-top/bottom.png`. Row order = board order = names, so **no pixel-matcher naming step needed** — batch-review.html is now only for the legacy `batch1-pNN.png` files (superseded for classics, ignorable). Old `(Bulk N) name` designs in Canva are the text-bearing v1 batch — trash freely; `(Bulk N) classics` are canonical.
  **SHAPED DONE (2026-07-21): 24 boards as `hs<N>-top/bottom.png` (48 files: hs1–hs21, hs22.5, hs23, hs24).** Second run refinements: name the sheet rows `hs<N>` directly and the generated designs inherit clean titles (no `(Bulk N)` indirection); export can be fully manual (Projects → select all → Download → transparent PNG zip) — no browser automation needed, only the slice script runs locally. **Recurring Canva Sheets gotcha: it duplicated one row's images again** (hs22 came out pixel-identical to hs13) — ALWAYS hash-check exported cards before slicing (`md5` the files, then pixel-hash the sliced halves; identical byte-size is the tell). The missing board was hand-fixed in the hs22 design and re-exported as `hs22.5` (name kept, it's a distinct board — white AVRG stamp; its cutout is larger, ~360×1000, because hand-placed images beat bulk-create's auto-fit). Loose thread: `~/Desktop/backgroundremovalcanva/shaped/` has 52 photos but only 24 boards made the sheet — 4 photos unaccounted (junk frames or up to 2 missed boards); check sheet vs folder next Canva session.
- Inventory-ID plumbing: k-home-dual's `CANVA` map (board→cutout file + canonical cl/hs ref) is a JS-side bridge. The permanent home is build_site.py → data.js: carry each board's inventory ID and cutout path in the data so every page gets them without a hand-kept map. Do this when porting to production `site/`. The mapping itself was derived by pixel-match + sheet-order (receipts in `card-lab/canva-review.html`); classics sheet order = site order with inventory boards interleaved.
- More board sections toward the end of the shop scroll — the full inventory goes up eventually, even the boards that aren't first-class citizens ("you never know what people will be into"). Bolt-on point undecided: a simple button somewhere, or something you discover at the bottom of the scroll. Fits naturally with the one-scroll shop model.
- Open question: should `.colpage-inner h2` match the Satoshi Black used on `.band h2`?
- Eventually: port the blackout draft into production `site/` (see path gotcha above).
